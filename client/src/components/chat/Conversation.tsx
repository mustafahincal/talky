import React from "react";
import { Container } from "react-bootstrap";

const Conversation = () => {
  return <Container className="bg-danger">Conversation</Container>;
};

export default Conversation;
